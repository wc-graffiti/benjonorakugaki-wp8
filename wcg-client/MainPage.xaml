<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wcg_client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    x:Class="wcg_client.MainPage"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.BottomAppBar>
        <CommandBar ClosedDisplayMode="Compact">
            <AppBarButton x:Name="rakugakiBtn"  Icon="Edit" Label="RAKUGAKI"/>
            <AppBarButton x:Name="reloadBtn" Icon="Refresh" Label="Reload"/>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid>
        <Maps:MapControl
            x:Name="mainMap"
            Margin="10"
            MapServiceToken="{StaticResource MapServiceToken}"
        />
    </Grid>
</Page>